Display Build Info on Login Page
Display the build timestamp and Git commit hash on the login page to help the user identify the current deployment version.

Proposed Changes
Configuration
[MODIFY] 
next.config.js
Add a helper to get the current Git commit hash.
Inject NEXT_PUBLIC_BUILD_TIME and NEXT_PUBLIC_GIT_COMMIT into the env configuration.
Frontend
[MODIFY] 
index.tsx
Replace the "Sign in to manage your blog" subtitle with a display of the build time and commit hash.
Format the date for better readability using date-fns if needed, or just a simple string.
Verification Plan
Automated Tests
Run npm run build to ensure the environment variables are correctly injected.
Start the app with npm run dev and check the login page.
Manual Verification
Verify that the login page shows a timestamp and a short git hash.
Confirm the values update when making a new commit and rebuilding.

Comment
⌥⌘M
